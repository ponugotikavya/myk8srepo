apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: myproject-ingress
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: myproject-service
            port:
              number: 80
